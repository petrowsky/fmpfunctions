/*
 * =====================================================
 * vars.makeLocal ( input )
 *
 * RETURNS:        (string) All global vars $$ converted to $ locals
 * DEPENDENCIES:   none 
 * NOTES:          
 * =====================================================
 * 
 */

Substitute ( input ; "$$" ; "$" )